plugins {
  id "com.diffplug.spotless" version "5.14.2"
}

subprojects {
  apply plugin: "com.diffplug.spotless"

  spotless {
    java {
      target 'src/*/java/**/*.java'

      removeUnusedImports()
      googleJavaFormat()
    }
  }
}